# ตัวอย่างภาษา AI สำหรับงาน Machine Learning

# เริ่มต้นโปรเจกต์แบบ Machine Learning
start create ML

# โหลดข้อมูล
load dataset "data/housing.csv" type "csv"

# ทำความสะอาดข้อมูล
clean data with drop_na true fill_mean "numeric" handle_outliers true

# แสดงข้อมูลตัวอย่าง
visualize data samples 5

# แบ่งข้อมูลสำหรับการฝึกและทดสอบ
split data into train, test with ratio 0.8, 0.2

# วิเคราะห์ความสัมพันธ์ของข้อมูล
visualize correlation_matrix
visualize feature_importance

# สร้างโมเดล Linear Regression
create model LinearRegression with normalize true fit_intercept true

# ฝึกโมเดล
train model on train_data with epochs 100 learning_rate 0.01 batch_size 32 validation true

# แสดงการเรียนรู้ของโมเดล
plot learning_curve
plot validation_curve

# ทดสอบประสิทธิภาพของโมเดล
evaluate model on test_data

# แสดงผลการประเมิน
show mae
show mse
show r2_score

# แสดงการเปรียบเทียบระหว่างค่าจริงและค่าทำนาย
visualize prediction_vs_actual

# บันทึกโมเดลที่ฝึกแล้ว
save model to "housing_price_predictor.pkl"

# ทดลองสร้างโมเดลแบบอื่น
create model RandomForest with n_estimators 100 max_depth 10

# ฝึกโมเดล
train model on train_data

# เปรียบเทียบโมเดล
compare models LinearRegression, RandomForest with metric "r2_score"