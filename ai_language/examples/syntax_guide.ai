
# ตัวอย่างลำดับการทำงานของ Syntax ภาษา AI

# 1. คำสั่งเริ่มต้น และสร้างโปรเจกต์ (เลือกได้ระหว่าง ML, DL, RL)
start create ML

# 2. คำสั่งโหลดข้อมูล
load dataset "datasets/linear_data.csv" type "csv"

# 3. คำสั่งทำความสะอาดข้อมูล
clean data with drop_na true

# 4. คำสั่งแบ่งข้อมูล
split data into train, test with ratio 0.8, 0.2

# 5. คำสั่งสร้างโมเดล
create model LinearRegression

# 6. คำสั่งปรับแต่งการเทรน (ไม่บังคับ)
set learning_rate 0.01
set epochs 100
set batch_size 32

# 7. คำสั่งเทรนโมเดล
train model

# 8. คำสั่งแสดงผลลัพธ์
show metric "accuracy"
show metric "loss"
visualize data

# 9. คำสั่งบันทึกโมเดล
save model to "trained_model.pkl"

# 10. คำสั่งโหลดโมเดล (ทางเลือกแทนการสร้างโมเดลใหม่)
# load model from "trained_model.pkl"
