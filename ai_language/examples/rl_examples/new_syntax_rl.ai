
# ตัวอย่างการใช้ syntax ใหม่กับโปรเจกต์ RL

# เริ่มต้นโปรเจกต์
start

# สร้างโปรเจกต์ RL
create RL

# โหลดข้อมูลสภาพแวดล้อม
load dataset "datasets/environment.json"

# สร้างสภาพแวดล้อมใหม่ (ตัวอย่างสำหรับ syntax ใหม่)
# create environment "maze"
# set environment parameter size 10x10

# สร้างโมเดล
create model QLearning

# ปรับแต่งพารามิเตอร์
set discount_factor 0.9
set episodes 1000
set exploration_rate 0.3
set timezone 7

# ฝึกโมเดล
train model

# ประเมินโมเดล
evaluate model

# แสดงผลลัพธ์
show performance

# ตรวจสอบสถานะปัจจุบัน
check status

# สร้างกราฟการเรียนรู้
plot model learning_curve "graphs/rl_learning_curve.png"

# บันทึกโมเดล (โปรแกรมจะเพิ่มนามสกุล .rlmodel ให้อัตโนมัติ)
save model "rl_model"

# แสดงรายการโมเดลที่มีอยู่
list models

# สิ้นสุดการทำงาน
end
